/**
 * phoenix deploy — deploy project to configured target.
 * Placeholder: actual deploy integration (e.g. Vercel) can be added later.
 */
export async function cmdDeploy(_opts) {
    console.error("Deploy is not yet implemented. Configure a deploy target in phoenix.config.json.");
    process.exit(1);
}
//# sourceMappingURL=deploy.js.map